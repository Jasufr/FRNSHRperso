<% content_for :meta_title, "FRNSHR - Room composer" %>
<div class="container">
  <div class="form-container new-room-form-container">
    <div class = "favoritecolor-card">
      <h2>Define Your Decor</h2>
      <%= simple_form_for(@room, html: { data: { controller: 'colorscheme' } }) do |f| %>
        <%= f.hidden_field :room_type, data: { colorscheme_target: 'room' } %>
        <div class="room-card-container">
          <% Room::ROOMS[0..-2].each do |room| %>
            <button data-action="click->colorscheme#roomchecking" data-roomtype="<%= room %>" class="room-card"
              style="background-image: url('<%= image_path("#{room}.jpg") %>'); ">
              <!-- Your button text or content -->
              <%= room.capitalize %>
            </button>
          <% end %>
        </div>
        <p>Main Tone Choice:</p>
        <%# <label for="colorselect"><i class="fa-solid fa-palette"></label> %>
        <input name="colorselect" type="color" data-colorscheme-target="usercolor" data-action="input->colorscheme#suggestion" class="favoritecolor" value="#FFFFFF">
        <div class = "schemetitle" data-colorscheme-target = "title"></div>
        <div class = "scheme-card-container" data-colorscheme-target = "displayArea">
        </div>
        <p class="design-your-room-btn"><%= f.button :submit, "Search Decor Options", class: "btn btn-primary text-white" %></p>
      </div>
    <% end %>
  </div>
</div>
